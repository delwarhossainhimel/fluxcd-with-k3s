apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: catch-all
  namespace: ingress-nginx
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: 192.168.20.74.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: dummy-backend
                port:
                  number: 80
