apiVersion: v1
kind: ConfigMap
metadata:
  name: wordpress-logs-dashboard
  namespace: flux-system
  labels:
    grafana_dashboard: "1"
data:
  wordpress-logs.json: |
    { ... your exported dashboard JSON ... }
